# Render 一键部署蓝图（https://render.com/deploy?repo=<本仓库地址>）
services:
  - type: web
    name: onepost
    runtime: docker
    rootDir: social-publisher
    plan: free
    # manifest.json 在登录豁免名单中，设置访问密码后健康检查仍可通过
    healthCheckPath: /manifest.json
